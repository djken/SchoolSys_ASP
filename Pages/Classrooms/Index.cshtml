@page
@model SchoolSysMo.Pages.Classrooms.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Classroom[0].Name)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Classroom) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.ClassroomId">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.ClassroomId">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.ClassroomId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
